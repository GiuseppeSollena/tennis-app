import { environment } from '../../../environments/environment';

export const ApiUrls = {
  teams: `${environment.apiUrl}/teams`,
  players: `${environment.apiUrl}/players`,
  // Aggiungi altri percorsi API qui
};
